{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Publish Scheduler Agent Input",
  "type": "object",
  "properties": {
    "content_id": {
      "type": "string",
      "description": "A unique identifier for the content to be published."
    },
    "publish_datetime": {
      "type": "string",
      "format": "date-time",
      "description": "The ISO 8601 datetime for when the content should be published."
    },
    "platform": {
      "type": "string",
      "enum": ["blog", "youtube", "twitter"],
      "description": "The platform where the content will be published."
    }
  },
  "required": ["content_id", "publish_datetime", "platform"]
}